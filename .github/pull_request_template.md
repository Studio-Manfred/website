## Summary
<!-- One or two sentences. What changed? -->

## Why
<!-- The motivating change, not the diff. Link a Linear ticket if there is one. -->

## Test plan
<!-- How did you verify this works? Manual + automated steps. -->
- [ ]
- [ ]

## Tests added
<!--
TDD rule (see CLAUDE.md): if the diff touches `lib/`, stateful components,
route handlers, data fetches, or any module with state / intervals / regex /
redirects, an accompanying test is required. Presentational JSX-only changes
are exempt.

Tick the box that matches this PR.
-->
- [ ] Unit / component / E2E tests added or extended.
- [ ] No new tests — purely presentational JSX, config, or docs.
- [ ] Waiver: tests intentionally skipped — see "Why" above for the reason.

## Coverage
<!--
The ratchet enforces that statements / branches / functions / lines never
drop more than 0.5% below `.coverage-baseline.json`. CI will fail if it does.
-->

## Linked issues
<!-- e.g. Closes STU-XXX, Refs STU-YYY -->
